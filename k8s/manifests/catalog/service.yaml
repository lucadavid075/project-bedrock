apiVersion: v1
kind: Service
metadata:
  name: catalog
  namespace: retail-app
  labels:
    app: catalog
    Project: karatu-2025-capstone
spec:
  selector:
    app: catalog
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
