apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: retail-app
  labels:
    app: orders
    Project: karatu-2025-capstone
spec:
  selector:
    app: orders
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
