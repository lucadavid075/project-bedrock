apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: retail-app
  labels:
    app: checkout
    Project: karatu-2025-capstone
spec:
  selector:
    app: checkout
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
